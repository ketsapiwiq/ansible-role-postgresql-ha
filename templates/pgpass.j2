{% for server in repmgr_target_group_hosts %}
{{ hostvars[server].ansible_hostname }}:{{ postgresql_port }}:{{ repmgr_database }}:{{ repmgr_user }}:{% for user in postgresql_users if user['name'] == repmgr_user %}{{user['pass']}}{% endfor %}
{{ hostvars[server].ansible_hostname }}:{{ postgresql_port }}:replication:{{repmgr_replication_user}}:{% for user in postgresql_users if user['name'] == repmgr_user %}{{user['pass']}}{% endfor %}
{% endfor %}